# Overlay for running the real-database integration tests locally:
#   docker compose -f docker-compose.yml -f docker-compose.test.yml up -d db
#   cd server && npm run test:db
# Publishes Postgres on 15432 (only while testing) so the suite can connect.
services:
  db:
    ports:
      - "15432:5432"
